import React from 'react';

const SubAnalysisMove = ({piece, to, ravNumber, raw, moveNumberOverall}) => {
    return (
        <div>
            {raw},
        </div>
    );
}

export default SubAnalysisMove;